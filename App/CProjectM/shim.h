#ifndef CPROJECTM_SHIM_H
#define CPROJECTM_SHIM_H

// Umbrella for the libprojectM 4.x C API (LGPL-2.1, dynamically linked).
// Headers live in vendor/dist/include (installed by scripts/build-libprojectm.sh).

#include <projectM-4/projectM.h>
#include <projectM-4/playlist.h>

#endif
